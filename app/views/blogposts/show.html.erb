<p id="notice"><%= notice %></p>

<div class="boxed">

<p> <h1> <%= @blogpost.title %> </h1> </p>

<p> <%= @blogpost.contents %> </p>
 </div>
<p>
<table>
  <thead>
    <tr>
      <th>Commenttext</th>
      <th>Author</th>
      <th>Blogpost</th>
    </tr>
  </thead>

  <tbody>
    <% @bpcomments.each do |comment| %>
      <tr>
	<td><%= comment.commenttext %></td>
        <td><%= comment.author %></td>
        <td><%= comment.blogpost_id %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</p>

  

<%= link_to 'Edit', edit_blogpost_path(@blogpost) %> |
<%= link_to 'Back', blogposts_path %>
